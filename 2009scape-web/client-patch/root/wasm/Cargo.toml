[package]
name = "wasm-rs"
version = "0.1.0"
authors = []

[dependencies]
wasm-bindgen = "0.2"
num-bigint = "0.4"

[lib]
crate-type = ["cdylib"]

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
